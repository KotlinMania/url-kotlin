# Rust callers — `url-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `url` | `url` | 61 | 23 | 11 | 24 |

## `url`

### Inbound symbols (6 distinct, 61 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `url`.

#### `url::Url` — 52 site(s) (20 codex, 32 other, 22 caller crates)

- ✅ `codex-kotlin/codex-api` `src/endpoint/realtime_websocket/methods.rs:46` — `use url::Url`
- ✅ `codex-kotlin/codex-api` `src/endpoint/responses_websocket.rs:47` — `use url::Url`
- ✅ `codex-kotlin/codex-api` `src/provider.rs:9` — `use url::Url`
- ✅ `codex-kotlin/codex-app-server-client` `src/remote.rs:47` — `use url::Url`
- ✅ `codex-kotlin/codex-app-server-test-client` `src/lib.rs:86` — `use url::Url`
- ✅ `codex-kotlin/codex-app-server-transport` `src/transport/remote_control/protocol.rs:8` — `use url::Url`
- ✅ `codex-kotlin/codex-core` `src/mcp_tool_call.rs:77` — `use url::Url`
- ✅ `codex-kotlin/codex-core-plugins` `src/remote.rs:21` — `use url::Url`
- ✅ `codex-kotlin/codex-core-plugins` `src/remote_bundle.rs:21` — `use url::Url`
- ✅ `codex-kotlin/codex-core-plugins` `src/remote_legacy.rs:7` — `use url::Url`
- ✅ `codex-kotlin/codex-device-key` `src/lib.rs:15` — `use url::Url`
- ✅ `codex-kotlin/codex-linux-sandbox` `src/proxy_routing.rs:24` — `use url::Url`
- _… and 40 more site(s)._

#### `url::Host` — 4 site(s) (4 codex, 0 other, 4 caller crates)

- ✅ `codex-kotlin/codex-app-server-transport` `src/transport/remote_control/protocol.rs:7` — `use url::Host`
- ✅ `codex-kotlin/codex-core-plugins` `src/remote_bundle.rs:20` — `use url::Host`
- ✅ `codex-kotlin/codex-device-key` `src/lib.rs:14` — `use url::Host`
- ✅ `codex-kotlin/codex-network-proxy` `src/policy.rs:14` — `use url::Host as UrlHost`

#### `url2::Url` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `schemars-kotlin/schemars` `src/json_schema_impls/url2.rs:4` — `use url2::Url`
- `schemars-kotlin/schemars` `tests/integration/url.rs:2` — `use url2::Url`

#### `url` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `oauth2-kotlin/oauth2` `src/lib.rs:554` — `pub_use url`

#### `url::form_urlencoded` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `oauth2-kotlin/oauth2` `src/endpoint.rs:10` — `use url::form_urlencoded`

#### `url::form_urlencoded::byte_serialize` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `oauth2-kotlin/oauth2` `src/code.rs:198` — `use url::form_urlencoded::byte_serialize`
